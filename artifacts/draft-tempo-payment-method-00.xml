<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE rfc [
  <!ENTITY nbsp "&#160;">
  <!ENTITY zwsp "&#8203;">
]>
<rfc xmlns:xi="http://www.w3.org/2001/XInclude"
     ipr="trust200902"
     docName="draft-tempo-payment-method-00"
     submissionType="IETF"
     category="info"
     consensus="true"
     version="3">

  <front>
    <title>Tempo Payment Method for HTTP Payment Authentication</title>
    <seriesInfo name="Internet-Draft" value="draft-tempo-payment-method-00"/>
    <author fullname="Jake Moxey">
      <address>
        <email>jake@tempo.xyz</email>
      </address>
    </author>
    <date/>
    <abstract>
      <t>This document defines the &quot;tempo&quot; payment method for use with the Payment
HTTP Authentication Scheme [I-D.ietf-httpauth-payment]. It specifies how
clients and servers exchange TIP-20 token payments on the Tempo blockchain,
supporting one-time charges, payment authorizations, and recurring subscriptions.</t>
    </abstract>
  </front>

  <middle>
    <section anchor="introduction">
      <name>Introduction</name>
      <t>The Tempo blockchain is a payments-focused EVM network with native support for stablecoin transactions, account abstraction, and programmable payment authorization. This specification defines how Tempo&apos;s payment primitives integrate with the Payment HTTP Authentication Scheme [I-D.ietf-httpauth-payment].</t>
      <t>Tempo provides two mechanisms for authorizing payments:</t>
      <ol type="1">
        <li><strong>Tempo Transactions</strong>: EIP-2718 type 0x76 transactions with TIP-20</li>
      </ol>
      <t>token operations (<tt>transfer</tt>, <tt>approve</tt>)</t>
      <ol type="1">
        <li><strong>Access Keys</strong>: Delegated signing keys with spending limits and expiry</li>
      </ol>
      <t>This specification supports three payment intents:</t>
      <ul>
        <li><strong>charge</strong>: One-time TIP-20 token transfer</li>
        <li><strong>authorize</strong>: Payment authorization with spending limits</li>
        <li><strong>subscription</strong>: Recurring payment authorization with periodic limits</li>
      </ul>
      <section anchor="tempo-payment-flow">
        <name>Tempo Payment Flow</name>
        <t>The following diagram illustrates the Tempo-specific payment flow:</t>
        <artwork type="ascii-art"><![CDATA[
   Client                                            Server
      │                                                 │
      │  (1) GET /resource                              │
      ├────────────────────────────────────────────────>│
      │                                                 │
      │  (2) 402 Payment Required                       │
      │      WWW-Authenticate: Payment method="tempo",  │
      │        intent="charge", request=<base64url>     │
      │<────────────────────────────────────────────────┤
      │                                                 │
      │  (3) Client signs Tempo Transaction or          │
      │      Key Authorization, or broadcasts tx        │
      │      and obtains hash                           │
      │                                                 │
      │  (4) GET /resource                              │
      │      Authorization: Payment <credential>        │
      ├────────────────────────────────────────────────>│
      │                                                 │
      │  (5) Server broadcasts transaction or verifies  │
      │      client-broadcast                           │
      │                                                 │
      │  (6) 200 OK                                     │
      │      Payment-Receipt: <receipt with txHash>     │
      │<────────────────────────────────────────────────┤
      │                                                 │
]]></artwork>
      </section>
      <section anchor="relationship-to-the-payment-scheme">
        <name>Relationship to the Payment Scheme</name>
        <t>This document is a payment method specification as defined in Section 10.1 of [I-D.ietf-httpauth-payment]. It defines the <tt>request</tt> and <tt>payload</tt> structures for the <tt>tempo</tt> payment method, along with verification and settlement procedures.</t>
      </section>
    </section>
    <section anchor="requirements-language">
      <name>Requirements Language</name>
      <t>The key words &quot;<bcp14>MUST</bcp14>&quot;, &quot;<bcp14>MUST NOT</bcp14>&quot;, &quot;<bcp14>REQUIRED</bcp14>&quot;, &quot;<bcp14>SHALL</bcp14>&quot;, &quot;<bcp14>SHALL NOT</bcp14>&quot;, &quot;<bcp14>SHOULD</bcp14>&quot;, &quot;<bcp14>SHOULD NOT</bcp14>&quot;, &quot;<bcp14>RECOMMENDED</bcp14>&quot;, &quot;<bcp14>NOT RECOMMENDED</bcp14>&quot;, &quot;<bcp14>MAY</bcp14>&quot;, and &quot;<bcp14>OPTIONAL</bcp14>&quot; in this document are to be interpreted as described in BCP 14 <xref target="RFC2119"/> <xref target="RFC8174"/> when, and only when, they appear in all capitals, as shown here.</t>
    </section>
    <section anchor="terminology">
      <name>Terminology</name>
      <dl>
        <dt>TIP-20</dt>
        <dd><t>Tempo&apos;s enshrined token standard, implemented as precompiles rather than smart contracts. TIP-20 tokens use 6 decimal places and provide <tt>transfer</tt>, <tt>transferFrom</tt>, and <tt>approve</tt> operations.</t></dd>
        <dt>Tempo Transaction</dt>
        <dd><t>An EIP-2718 transaction with type prefix <tt>0x76</tt>, supporting batched calls, multiple signature types (secp256k1, P256, WebAuthn), 2D nonces, and validity windows.</t></dd>
        <dt>Access Key</dt>
        <dd><t>A delegated signing key. Access keys may have an expiry timestamp and  a per-token spending limit.</t></dd>
        <dt>2D Nonce</dt>
        <dd><t>Tempo&apos;s nonce system where each account has multiple independent nonce lanes (<tt>nonce_key</tt>), enabling parallel transaction submission.</t></dd>
        <dt>Fee Payer</dt>
        <dd><t>An account that pays transaction fees on behalf of another account. Tempo Transactions support fee payment via a separate signature domain (<tt>0x78</tt>), allowing the server to pay for fees while the client only signs the payment authorization.</t></dd>
      </dl>
    </section>
    <section anchor="method-identifier">
      <name>Method Identifier</name>
      <t>This specification registers the following payment method identifier:</t>
      <sourcecode><![CDATA[
tempo
]]></sourcecode>
      <t>The identifier is case-sensitive and <bcp14>MUST</bcp14> be lowercase. No sub-methods are defined by this specification.</t>
    </section>
    <section anchor="payment-intents">
      <name>Payment Intents</name>
      <t>This specification defines three payment intents for use with the <tt>tempo</tt> payment method. These intents are registered in the Payment Intent Registry per Section 13.4 of [I-D.ietf-httpauth-payment].</t>
      <section anchor="intent-charge">
        <name>Intent: &quot;charge&quot;</name>
        <t>A one-time payment of the specified amount. The server may submit the signed transaction any time before the <tt>expires</tt> timestamp.</t>
        <t>Fulfillment mechanisms:</t>
        <ol type="1">
          <li><strong>Tempo Transaction with <tt>transfer</tt></strong>: The payer signs a Tempo</li>
        </ol>
        <t>Transaction calling <tt>transfer(recipient, amount)</tt> on the specified    TIP-20 token.</t>
      </section>
      <section anchor="intent-authorize">
        <name>Intent: &quot;authorize&quot;</name>
        <t>A payment authorization. The payer grants the server permission to charge up to the specified amount before the expiry timestamp.</t>
        <t>Required parameters:</t>
        <ul>
          <li>Expiry timestamp (<bcp14>MUST</bcp14> be a reasonable future time)</li>
          <li>Maximum amount (spending limit)</li>
        </ul>
        <t>Fulfillment mechanisms:</t>
        <ol type="1">
          <li><strong>Tempo Transaction with <tt>approve</tt></strong>: The payer signs a Tempo</li>
        </ol>
        <t>Transaction calling <tt>approve(spender, amount)</tt> on the TIP-20 token,    with <tt>validBefore</tt> set to the requested expiry.</t>
        <ol type="1">
          <li><strong>Access Key with expiry and limit</strong>: The payer provisions an access</li>
        </ol>
        <t>key with the requested expiry timestamp and a spending limit for the    specified token and amount.</t>
      </section>
      <section anchor="intent-subscription">
        <name>Intent: &quot;subscription&quot;</name>
        <t>A recurring payment authorization. The payer grants the server permission to charge a specified amount per period (e.g., daily, weekly, monthly).</t>
        <t>Required parameters:</t>
        <ul>
          <li>Period duration in seconds</li>
          <li>Maximum amount per period</li>
          <li>Optional: Expiry timestamp</li>
        </ul>
        <t>Fulfillment mechanism:</t>
        <ul>
          <li><strong>Access Key with periodic limits</strong>: The payer provisions an access key</li>
        </ul>
        <t>with a recurring spending limit. The limit resets after each period.</t>
      </section>
    </section>
    <section anchor="request-schema">
      <name>Request Schema</name>
      <t>The <tt>request</tt> parameter in the <tt>WWW-Authenticate</tt> challenge contains a base64url-encoded JSON object. The schema is determined by the <tt>intent</tt> parameter in the challenge. Clients parse the request and construct the appropriate Tempo Transaction or Key Authorization to fulfill it.</t>
      <section anchor="charge-request">
        <name>Charge Request</name>
        <t>For <tt>intent=&quot;charge&quot;</tt>, the request specifies a one-time payment:</t>
        <table>
          <thead>
            <tr>
              <th>Field</th>
              <th>Type</th>
              <th>Required</th>
              <th>Description</th>
            </tr>
          </thead>
          <tbody>
            <tr>
              <td><tt>amount</tt></td>
              <td>string</td>
              <td><bcp14>REQUIRED</bcp14></td>
              <td>Amount in base units (stringified number)</td>
            </tr>
            <tr>
              <td><tt>asset</tt></td>
              <td>string</td>
              <td><bcp14>REQUIRED</bcp14></td>
              <td>TIP-20 token address</td>
            </tr>
            <tr>
              <td><tt>destination</tt></td>
              <td>string</td>
              <td><bcp14>REQUIRED</bcp14></td>
              <td>Recipient address</td>
            </tr>
            <tr>
              <td><tt>expires</tt></td>
              <td>string</td>
              <td><bcp14>REQUIRED</bcp14></td>
              <td>Expiry timestamp in ISO 8601 format</td>
            </tr>
            <tr>
              <td><tt>feePayer</tt></td>
              <td>boolean</td>
              <td><bcp14>OPTIONAL</bcp14></td>
              <td>If <tt>true</tt>, server will pay transaction fees (default: <tt>false</tt>)</td>
            </tr>
          </tbody>
        </table>
        <t>Example:</t>
        <sourcecode type="json"><![CDATA[
{
  "amount": "1000000",
  "asset": "0x20c0000000000000000000000000000000000001",
  "destination": "0x742d35Cc6634C0532925a3b844Bc9e7595f8fE00",
  "expires": "2025-01-06T12:00:00Z",
  "feePayer": true
}
]]></sourcecode>
        <t>The client fulfills this by signing a Tempo Transaction with <tt>transfer(destination, amount)</tt> on the specified <tt>asset</tt>, with <tt>validBefore</tt> set to <tt>expires</tt>. The client <bcp14>SHOULD</bcp14> use a dedicated <tt>nonceKey</tt> (2D nonce lane) for payment transactions to avoid blocking other account activity if the transaction is not immediately settled.</t>
        <t>If <tt>feePayer</tt> is <tt>true</tt>, the client signs with <tt>fee_payer_signature</tt> set to <tt>0x00</tt> and <tt>fee_token</tt> empty, allowing the server to sponsor fees. If <tt>feePayer</tt> is <tt>false</tt> or omitted, the client <bcp14>MUST</bcp14> set <tt>fee_token</tt> and pay fees themselves.</t>
      </section>
      <section anchor="authorize-request">
        <name>Authorize Request</name>
        <t>For <tt>intent=&quot;authorize&quot;</tt>, the request specifies a payment authorization:</t>
        <table>
          <thead>
            <tr>
              <th>Field</th>
              <th>Type</th>
              <th>Required</th>
              <th>Description</th>
            </tr>
          </thead>
          <tbody>
            <tr>
              <td><tt>asset</tt></td>
              <td>string</td>
              <td><bcp14>REQUIRED</bcp14></td>
              <td>TIP-20 token address</td>
            </tr>
            <tr>
              <td><tt>destination</tt></td>
              <td>string</td>
              <td><bcp14>OPTIONAL</bcp14></td>
              <td>Authorized spender address (required for transaction fulfillment)</td>
            </tr>
            <tr>
              <td><tt>expires</tt></td>
              <td>string</td>
              <td><bcp14>REQUIRED</bcp14></td>
              <td>Expiry timestamp in ISO 8601 format</td>
            </tr>
            <tr>
              <td><tt>feePayer</tt></td>
              <td>boolean</td>
              <td><bcp14>OPTIONAL</bcp14></td>
              <td>If <tt>true</tt>, server will pay transaction fees (default: <tt>false</tt>)</td>
            </tr>
            <tr>
              <td><tt>limit</tt></td>
              <td>string</td>
              <td><bcp14>REQUIRED</bcp14></td>
              <td>Maximum spend amount in base units (stringified number)</td>
            </tr>
            <tr>
              <td><tt>validFrom</tt></td>
              <td>string</td>
              <td><bcp14>OPTIONAL</bcp14></td>
              <td>Start timestamp in ISO 8601 format</td>
            </tr>
          </tbody>
        </table>
        <t>Example:</t>
        <sourcecode type="json"><![CDATA[
{
  "asset": "0x20c0000000000000000000000000000000000001",
  "expires": "2025-02-05T12:00:00Z",
  "feePayer": true,
  "limit": "50000000"
}
]]></sourcecode>
        <t>The client fulfills this by either:</t>
        <ol type="1">
          <li>Signing a Tempo Transaction with <tt>approve(destination, limit)</tt> on the</li>
        </ol>
        <t>specified <tt>asset</tt>, with <tt>validBefore</tt> set to <tt>expires</tt> and optionally    <tt>validAfter</tt> set to <tt>validFrom</tt>. The <tt>destination</tt> field <bcp14>MUST</bcp14> be present    in the request when using transaction fulfillment. The client <bcp14>SHOULD</bcp14> use    a dedicated <tt>nonceKey</tt> (2D nonce lane) for payment transactions.</t>
        <ol type="1">
          <li>Signing a Key Authorization with expiry = <tt>expires</tt> and a spending limit</li>
        </ol>
        <t>= <tt>limit</tt> for the specified <tt>asset</tt></t>
        <t>If <tt>feePayer</tt> is <tt>true</tt>, the client signs with <tt>fee_payer_signature</tt> set to <tt>0x00</tt> and <tt>fee_token</tt> empty. If <tt>feePayer</tt> is <tt>false</tt> or omitted, the client <bcp14>MUST NOT</bcp14> sign a key authorization; the client <bcp14>MUST</bcp14> sign a transaction with <tt>fee_token</tt> set to pay fees themselves.</t>
      </section>
      <section anchor="subscription-request">
        <name>Subscription Request</name>
        <t>For <tt>intent=&quot;subscription&quot;</tt>, the request specifies recurring authorization:</t>
        <table>
          <thead>
            <tr>
              <th>Field</th>
              <th>Type</th>
              <th>Required</th>
              <th>Description</th>
            </tr>
          </thead>
          <tbody>
            <tr>
              <td><tt>amount</tt></td>
              <td>string</td>
              <td><bcp14>REQUIRED</bcp14></td>
              <td>Amount per period in base units (stringified number)</td>
            </tr>
            <tr>
              <td><tt>asset</tt></td>
              <td>string</td>
              <td><bcp14>REQUIRED</bcp14></td>
              <td>TIP-20 token address</td>
            </tr>
            <tr>
              <td><tt>expires</tt></td>
              <td>string</td>
              <td><bcp14>REQUIRED</bcp14></td>
              <td>Total expiry timestamp in ISO 8601 format</td>
            </tr>
            <tr>
              <td><tt>period</tt></td>
              <td>string</td>
              <td><bcp14>REQUIRED</bcp14></td>
              <td>Period duration in seconds (stringified number)</td>
            </tr>
            <tr>
              <td><tt>validFrom</tt></td>
              <td>string</td>
              <td><bcp14>OPTIONAL</bcp14></td>
              <td>Start timestamp in ISO 8601 format</td>
            </tr>
          </tbody>
        </table>
        <t>Example:</t>
        <sourcecode type="json"><![CDATA[
{
  "amount": "10000000",
  "asset": "0x20c0000000000000000000000000000000000001",
  "expires": "2026-01-06T00:00:00Z",
  "period": "2592000"
}
]]></sourcecode>
        <t>The <tt>period</tt> value <tt>2592000</tt> represents 30 days in seconds.</t>
        <t>The client fulfills this by signing a Key Authorization with:</t>
        <ul>
          <li>Expiry = <tt>expires</tt></li>
          <li>Periodic spending limit = <tt>amount</tt> per <tt>period</tt> for the specified <tt>asset</tt></li>
        </ul>
        <t>Tempo Transactions cannot fulfill subscription intents because ERC-20 style approvals do not support periodic limit semantics.</t>
      </section>
    </section>
    <section anchor="credential-schema">
      <name>Credential Schema</name>
      <t>The credential in the <tt>Authorization</tt> header contains a base64url-encoded JSON object per Section 5.2 of [I-D.ietf-httpauth-payment].</t>
      <section anchor="credential-structure">
        <name>Credential Structure</name>
        <table>
          <thead>
            <tr>
              <th>Field</th>
              <th>Type</th>
              <th>Required</th>
              <th>Description</th>
            </tr>
          </thead>
          <tbody>
            <tr>
              <td><tt>id</tt></td>
              <td>string</td>
              <td><bcp14>REQUIRED</bcp14></td>
              <td>Challenge ID from the server&apos;s <tt>WWW-Authenticate</tt> header</td>
            </tr>
            <tr>
              <td><tt>payload</tt></td>
              <td>object</td>
              <td><bcp14>REQUIRED</bcp14></td>
              <td>Tempo-specific payload object</td>
            </tr>
            <tr>
              <td><tt>source</tt></td>
              <td>string</td>
              <td><bcp14>OPTIONAL</bcp14></td>
              <td>Payer identifier as a DID (e.g., <tt>did:pkh:eip155:42431:0x...</tt>)</td>
            </tr>
          </tbody>
        </table>
        <t>The <tt>source</tt> field, if present, <bcp14>SHOULD</bcp14> use the <tt>did:pkh</tt> method with the Tempo chain ID (42431 for Moderato testnet) and the payer&apos;s Ethereum address.</t>
      </section>
      <section anchor="payload-structure">
        <name>Payload Structure</name>
        <t>The <tt>payload</tt> object contains:</t>
        <table>
          <thead>
            <tr>
              <th>Field</th>
              <th>Type</th>
              <th>Required</th>
              <th>Description</th>
            </tr>
          </thead>
          <tbody>
            <tr>
              <td><tt>hash</tt></td>
              <td>string</td>
              <td>CONDITIONAL</td>
              <td>Transaction hash (required for <tt>hash</tt> type)</td>
            </tr>
            <tr>
              <td><tt>signature</tt></td>
              <td>string</td>
              <td>CONDITIONAL</td>
              <td>Hex-encoded RLP-serialized signed data (required for <tt>transaction</tt> and <tt>keyAuthorization</tt>)</td>
            </tr>
            <tr>
              <td><tt>type</tt></td>
              <td>string</td>
              <td><bcp14>REQUIRED</bcp14></td>
              <td>Fulfillment type: <tt>&quot;transaction&quot;</tt>, <tt>&quot;keyAuthorization&quot;</tt>, or <tt>&quot;hash&quot;</tt></td>
            </tr>
          </tbody>
        </table>
        <t>Either <tt>signature</tt> or <tt>hash</tt> <bcp14>MUST</bcp14> be present, depending on the <tt>type</tt>.</t>
      </section>
      <section anchor="transaction-payload">
        <name>Transaction Payload</name>
        <t>When <tt>type</tt> is <tt>&quot;transaction&quot;</tt>, <tt>signature</tt> contains the complete signed Tempo Transaction (type 0x76) serialized as RLP and hex-encoded with <tt>0x</tt> prefix. The transaction <bcp14>MUST</bcp14> contain the appropriate TIP-20 call:</t>
        <ul>
          <li>For <tt>charge</tt>: <tt>transfer(recipient, amount)</tt></li>
          <li>For <tt>authorize</tt>: <tt>approve(spender, amount)</tt></li>
        </ul>
        <t>Example:</t>
        <sourcecode type="json"><![CDATA[
{
  "id": "kM9xPqWvT2nJrHsY4aDfEb",
  "payload": {
    "signature": "0x76f901...signed transaction bytes...",
    "type": "transaction"
  },
  "source": "did:pkh:eip155:42431:0x1234567890abcdef1234567890abcdef12345678"
}
]]></sourcecode>
      </section>
      <section anchor="key-authorization-payload">
        <name>Key Authorization Payload</name>
        <t>When <tt>type</tt> is <tt>&quot;keyAuthorization&quot;</tt>, <tt>signature</tt> contains the complete signed Key Authorization serialized as RLP and hex-encoded with <tt>0x</tt> prefix. The authorization is signed by the root account and grants the access key permission to sign transactions on its behalf.</t>
        <t>Example:</t>
        <sourcecode type="json"><![CDATA[
{
  "id": "kM9xPqWvT2nJrHsY4aDfEb",
  "payload": {
    "signature": "0xf8...signed authorization bytes...",
    "type": "keyAuthorization"
  }
}
]]></sourcecode>
      </section>
      <section anchor="hash-payload">
        <name>Hash Payload</name>
        <t>When <tt>type</tt> is <tt>&quot;hash&quot;</tt>, the client has already broadcast the transaction to the Tempo network. The <tt>hash</tt> field contains the transaction hash for the server to verify onchain. This allows clients who prefer to manage their own transaction submission (e.g., for gas management or privacy reasons) to still use the Payment scheme.</t>
        <table>
          <thead>
            <tr>
              <th>Field</th>
              <th>Type</th>
              <th>Required</th>
              <th>Description</th>
            </tr>
          </thead>
          <tbody>
            <tr>
              <td><tt>hash</tt></td>
              <td>string</td>
              <td><bcp14>REQUIRED</bcp14></td>
              <td>Transaction hash with <tt>0x</tt> prefix</td>
            </tr>
            <tr>
              <td><tt>type</tt></td>
              <td>string</td>
              <td><bcp14>REQUIRED</bcp14></td>
              <td><tt>&quot;hash&quot;</tt></td>
            </tr>
          </tbody>
        </table>
        <t>Applicable intents:</t>
        <ul>
          <li><tt>charge</tt>: The hash references a <tt>transfer</tt> transaction</li>
          <li><tt>authorize</tt>: The hash references an <tt>approve</tt> transaction</li>
        </ul>
        <t>Note: <tt>subscription</tt> intent cannot use hash payloads because it requires a key authorization, not a transaction.</t>
        <t>Example:</t>
        <sourcecode type="json"><![CDATA[
{
  "id": "kM9xPqWvT2nJrHsY4aDfEb",
  "payload": {
    "hash": "0x1a2b3c4d5e6f7890abcdef1234567890abcdef1234567890abcdef1234567890",
    "type": "hash"
  },
  "source": "did:pkh:eip155:42431:0x1234567890abcdef1234567890abcdef12345678"
}
]]></sourcecode>
      </section>
    </section>
    <section anchor="verification-procedure">
      <name>Verification Procedure</name>
      <t>Servers <bcp14>MUST</bcp14> verify credentials before granting access or initiating settlement.</t>
      <section anchor="credential-verification">
        <name>Credential Verification</name>
        <t>For all credentials:</t>
        <ol type="1">
          <li>Decode the base64url credential and parse as JSON</li>
          <li>Verify <tt>id</tt> matches a valid, unexpired, unused challenge</li>
          <li>Verify <tt>type</tt> is <tt>&quot;transaction&quot;</tt>, <tt>&quot;keyAuthorization&quot;</tt>, or <tt>&quot;hash&quot;</tt></li>
          <li>For <tt>transaction</tt> and <tt>keyAuthorization</tt>: decode the hex-encoded <tt>signature</tt> field</li>
          <li>For <tt>hash</tt>: validate the <tt>hash</tt> field format (66 hex characters with <tt>0x</tt> prefix)</li>
        </ol>
      </section>
      <section anchor="payload-verification">
        <name>Payload Verification</name>
        <t>For <tt>type=&quot;transaction&quot;</tt>, servers <bcp14>MUST</bcp14> deserialize and verify the Tempo Transaction per [TEMPO-TX-SPEC].</t>
        <t>For <tt>type=&quot;keyAuthorization&quot;</tt>, servers <bcp14>MUST</bcp14> deserialize and verify the Key Authorization per [TEMPO-TX-SPEC].</t>
        <t>For <tt>type=&quot;hash&quot;</tt>, servers <bcp14>MUST</bcp14> verify the transaction onchain per Section 8.3.</t>
        <t>Servers <bcp14>SHOULD</bcp14> additionally verify that the transaction or authorization parameters match the original request (asset, amount, destination, expiry).</t>
      </section>
      <section anchor="hash-verification">
        <name>Hash Verification</name>
        <t>For <tt>type=&quot;hash&quot;</tt>, servers <bcp14>MUST</bcp14> verify the transaction onchain:</t>
        <ol type="1">
          <li>Call <tt>eth_getTransactionReceipt(hash)</tt> to retrieve the transaction receipt</li>
          <li>Verify the transaction status is <tt>1</tt> (success)</li>
          <li>Verify the transaction <tt>chainId</tt> matches the expected chain ID</li>
          <li>Parse the transaction receipt logs to verify the operation matches the intent:</li>
        </ol>
        <t>- For <tt>charge</tt>: verify a <tt>Transfer(from, to, amount)</tt> event on the correct      asset with the expected <tt>to</tt> (destination) and <tt>amount</tt>    - For <tt>authorize</tt>: verify an <tt>Approval(owner, spender, amount)</tt> event on      the correct asset with the expected <tt>spender</tt> and <tt>amount</tt></t>
        <ol type="1">
          <li>Verify the <tt>from</tt> address matches the expected payer (if <tt>source</tt> is provided)</li>
          <li>Verify the transaction block timestamp is within the challenge validity window</li>
          <li>For <tt>authorize</tt> intent, servers <bcp14>SHOULD</bcp14> verify the payer&apos;s token balance is</li>
        </ol>
        <t>sufficient for expected future charges</t>
        <t>If the transaction is not yet confirmed (receipt not available), servers  <bcp14>SHOULD</bcp14> return <tt>202 Accepted</tt> with a <tt>Retry-After</tt> header.  Servers <bcp14>MAY</bcp14> poll up to a reasonable timeout (e.g., 30 seconds) before responding.</t>
      </section>
    </section>
    <section anchor="settlement-procedure">
      <name>Settlement Procedure</name>
      <t>Settlement converts a verified credential into actual token transfer.</t>
      <section anchor="fee-payment">
        <name>Fee Payment</name>
        <t>When a request includes <tt>feePayer: true</tt>, the server commits to paying transaction fees on behalf of the client. This allows clients to complete payments without holding fee tokens.</t>
        <section anchor="server-paid-fees">
          <name>Server-Paid Fees</name>
          <t>When <tt>feePayer: true</tt>:</t>
          <ol type="1">
            <li><strong>Client signs with placeholder</strong>: The client signs the Tempo Transaction</li>
          </ol>
          <t>with <tt>fee_payer_signature</tt> set to a placeholder value (<tt>0x00</tt>) and    <tt>fee_token</tt> left empty. The client uses signature domain <tt>0x76</tt>.</t>
          <ol type="1">
            <li><strong>Server receives credential</strong>: The server extracts the client-signed</li>
          </ol>
          <t>transaction from the credential payload.</t>
          <ol type="1">
            <li><strong>Server adds fee payment signature</strong>: The server selects a <tt>fee_token</tt> (any</li>
          </ol>
          <t>USD-denominated TIP-20 stablecoin) and signs the transaction using    signature domain <tt>0x78</tt>. This signature commits to the transaction    including the <tt>fee_token</tt> and client&apos;s address.</t>
          <ol type="1">
            <li><strong>Server broadcasts</strong>: The final transaction contains both signatures:</li>
          </ol>
          <t>- Client&apos;s signature (authorizing the payment)    - Server&apos;s <tt>fee_payer_signature</tt> (committing to pay fees)</t>
        </section>
        <section anchor="client-paid-fees">
          <name>Client-Paid Fees</name>
          <t>When <tt>feePayer: false</tt> or omitted, the client <bcp14>MUST</bcp14> set <tt>fee_token</tt> to a valid USD TIP-20 token address and pay fees themselves. The server broadcasts the transaction as-is without adding a fee payer signature.</t>
        </section>
        <section anchor="server-requirements">
          <name>Server Requirements</name>
          <t>When acting as fee payer, servers:</t>
          <ul>
            <li><bcp14>MUST</bcp14> maintain sufficient balance of a USD TIP-20 token to pay</li>
          </ul>
          <t>transaction fees</t>
          <ul>
            <li><bcp14>MAY</bcp14> use any USD-denominated TIP-20 token with sufficient AMM</li>
          </ul>
          <t>liquidity as the fee token</t>
          <ul>
            <li><bcp14>MAY</bcp14> recover fee costs through pricing or other business logic</li>
          </ul>
        </section>
        <section anchor="client-requirements">
          <name>Client Requirements</name>
          <ul>
            <li>When <tt>feePayer: true</tt>: Clients <bcp14>MUST</bcp14> sign with <tt>fee_payer_signature</tt> set</li>
          </ul>
          <t>to <tt>0x00</tt> and <tt>fee_token</tt> empty or <tt>0x80</tt> (RLP null)</t>
          <ul>
            <li>When <tt>feePayer: false</tt> or omitted: Clients <bcp14>MUST</bcp14> set <tt>fee_token</tt> to a</li>
          </ul>
          <t>valid USD TIP-20 token and have sufficient balance to pay fees</t>
        </section>
      </section>
      <section anchor="charge-settlement-transaction">
        <name>Charge Settlement (Transaction)</name>
        <t>For <tt>intent=&quot;charge&quot;</tt> fulfilled via transaction, the client signs a transaction containing the <tt>transfer</tt> call. If <tt>feePayer: true</tt>, the server adds its fee payer signature before broadcasting:</t>
        <artwork type="ascii-art"><![CDATA[
   Client                           Server                        Tempo Network
      │                                │                                │
      │  (1) Authorization:            │                                │
      │      Payment <credential>      │                                │
      ├───────────────────────────────>│                                │
      │                                │                                │
      │                                │  (2) If feePayer: true,        │
      │                                │      add fee payment signature │
      │                                │                                │
      │                                │  (3) eth_sendRawTxSync         │
      │                                ├───────────────────────────────>│
      │                                │                                │
      │                                │  (4) Transfer executed         │
      │                                │      (~500ms finality)         │
      │                                │<───────────────────────────────┤
      │                                │                                │
      │  (5) 200 OK                    │                                │
      │      Payment-Receipt: <txHash> │                                │
      │<───────────────────────────────┤                                │
      │                                │                                │
]]></artwork>
        <ol type="1">
          <li>Client submits credential containing signed <tt>transfer</tt> transaction</li>
          <li>If <tt>feePayer: true</tt>, server adds fee sponsorship (signs with <tt>0x78</tt> domain)</li>
          <li>Server broadcasts transaction to Tempo</li>
          <li>Transaction included in block with immediate finality (~500ms)</li>
          <li>Server returns receipt with transaction hash</li>
        </ol>
      </section>
      <section anchor="subscription-settlement-key-authorization">
        <name>Subscription Settlement (Key Authorization)</name>
        <t>For <tt>intent=&quot;subscription&quot;</tt> fulfilled via key authorization, the client signs an authorization granting the server permission to charge periodically:</t>
        <artwork type="ascii-art"><![CDATA[
   Client                        Server                     Tempo Network
      │                             │                             │
      │  (1) Authorization:         │                             │
      │      Payment <credential>   │                             │
      │      (signed keyAuth)       │                             │
      ├────────────────────────────>│                             │
      │                             │                             │
      │                             │  (2) Construct tx with:     │
      │                             │      - keyAuthorization     │
      │                             │      - transfer(amt) call   │
      │                             │                             │
      │                             │  (3) eth_sendRawTxSync      │
      │                             ├────────────────────────────>│
      │                             │                             │
      │                             │  (4) Key registered +       │
      │                             │      transfer executed      │
      │                             │<────────────────────────────┤
      │                             │                             │
      │  (5) 200 OK                 │                             │
      │      Payment-Receipt:       │                             │
      │      <txHash>               │                             │
      │<────────────────────────────┤                             │
      │                             │                             │
      │         ... 30 days pass ...                              │
      │                             │                             │
      │                             │  (6) Construct tx with:     │
      │                             │      - transfer(amt) call   │
      │                             │      (key already registered)│
      │                             │                             │
      │                             │  (7) eth_sendRawTxSync      │
      │                             ├────────────────────────────>│
      │                             │                             │
      │                             │  (8) Transfer executed      │
      │                             │<────────────────────────────┤
      │                             │                             │
      │         ... repeat each period until expiry ...           │
      │                             │                             │
]]></artwork>
        <ol type="1">
          <li>Client submits credential containing signed key authorization</li>
          <li>Server constructs transaction with <tt>keyAuthorization</tt> and first <tt>transfer</tt></li>
          <li>Transaction registers the key and executes the first charge</li>
          <li>Server returns receipt (subscription is now active)</li>
          <li>When each period elapses, server charges using the registered key</li>
          <li>Process repeats each period until authorization expires</li>
        </ol>
      </section>
      <section anchor="authorize-settlement-transaction">
        <name>Authorize Settlement (Transaction)</name>
        <t>For <tt>intent=&quot;authorize&quot;</tt> fulfilled via transaction, the client signs an <tt>approve</tt> transaction granting the server a spending allowance. If <tt>feePayer: true</tt>, the server adds its fee payer signature before broadcasting:</t>
        <artwork type="ascii-art"><![CDATA[
   Client                           Server                        Tempo Network
      │                                │                                │
      │  (1) Authorization:            │                                │
      │      Payment <credential>      │                                │
      ├───────────────────────────────>│                                │
      │                                │                                │
      │                                │  (2) If feePayer: true,        │
      │                                │      add fee payment signature │
      │                                │                                │
      │                                │  (3) eth_sendRawTxSync         │
      │                                ├───────────────────────────────>│
      │                                │                                │
      │                                │  (4) Approval granted          │
      │                                │<───────────────────────────────┤
      │                                │                                │
      │  (5) 200 OK                    │                                │
      │      Payment-Receipt: <txHash> │                                │
      │<───────────────────────────────┤                                │
      │                                │                                │
      │         ... later, when service is consumed ...                 │
      │                                │                                │
      │                                │  (6) transferFrom(client,      │
      │                                │      server, amount)           │
      │                                ├───────────────────────────────>│
      │                                │                                │
      │                                │  (7) Transfer executed         │
      │                                │<───────────────────────────────┤
      │                                │                                │
]]></artwork>
        <ol type="1">
          <li>Client submits credential containing signed <tt>approve</tt> transaction</li>
          <li>If <tt>feePayer: true</tt>, server adds fee sponsorship (signs with <tt>0x78</tt> domain)</li>
          <li>Server broadcasts transaction; approval registered onchain</li>
          <li>Server returns receipt (approval is now active)</li>
          <li>Later, when the server needs to charge, it calls <tt>transferFrom</tt></li>
          <li>Charges can occur up to the approved limit before expiry</li>
        </ol>
      </section>
      <section anchor="authorize-settlement-key-authorization">
        <name>Authorize Settlement (Key Authorization)</name>
        <t>For <tt>intent=&quot;authorize&quot;</tt> fulfilled via key authorization, the client signs an authorization granting the server permission to charge up to a limit:</t>
        <artwork type="ascii-art"><![CDATA[
   Client                        Server                     Tempo Network
      │                             │                             │
      │  (1) Authorization:         │                             │
      │      Payment <credential>   │                             │
      │      (signed keyAuth)       │                             │
      ├────────────────────────────>│                             │
      │                             │                             │
      │                             │  (2) Store keyAuth          │
      │                             │                             │
      │  (3) 200 OK                 │                             │
      │      (approval active)      │                             │
      │<────────────────────────────┤                             │
      │                             │                             │
      │         ... later, when service is consumed ...           │
      │                             │                             │
      │                             │  (4) Construct tx with:     │
      │                             │      - keyAuthorization     │
      │                             │      - transfer(amt) call   │
      │                             │                             │
      │                             │  (5) eth_sendRawTxSync      │
      │                             ├────────────────────────────>│
      │                             │                             │
      │                             │  (6) Key registered +       │
      │                             │      transfer executed      │
      │                             │<────────────────────────────┤
      │                             │                             │
]]></artwork>
        <ol type="1">
          <li>Client submits credential containing signed key authorization</li>
          <li>Server stores the authorization for future use</li>
          <li>Server grants access (approval is now active)</li>
          <li>Later, when the server needs to charge, it constructs a transaction</li>
        </ol>
        <t>with the <tt>keyAuthorization</tt> and <tt>transfer</tt> call</t>
        <ol type="1">
          <li>Charges can occur up to the authorized limit before expiry</li>
        </ol>
      </section>
      <section anchor="hash-settlement">
        <name>Hash Settlement</name>
        <t>For credentials with <tt>type=&quot;hash&quot;</tt>, the client has already broadcast the transaction. The server verifies the transaction onchain rather than broadcasting it:</t>
        <artwork type="ascii-art"><![CDATA[
   Client                        Server                     Tempo Network
      │                             │                             │
      │  (1) Broadcast tx           │                             │
      ├──────────────────────────────────────────────────────────>│
      │                             │                             │
      │  (2) Transaction confirmed  │                             │
      │<──────────────────────────────────────────────────────────┤
      │                             │                             │
      │  (3) Authorization:         │                             │
      │      Payment <credential>   │                             │
      │      (with txHash)          │                             │
      ├────────────────────────────>│                             │
      │                             │                             │
      │                             │  (4) eth_getTransactionReceipt
      │                             ├────────────────────────────>│
      │                             │                             │
      │                             │  (5) Receipt returned       │
      │                             │<────────────────────────────┤
      │                             │                             │
      │                             │  (6) Verify receipt         │
      │                             │                             │
      │  (7) 200 OK                 │                             │
      │      Payment-Receipt:       │                             │
      │      <txHash>               │                             │
      │<────────────────────────────┤                             │
      │                             │                             │
]]></artwork>
        <ol type="1">
          <li>Client constructs and broadcasts transaction directly to Tempo</li>
          <li>Client waits for transaction confirmation (optional but recommended)</li>
          <li>Client submits credential with the transaction hash</li>
          <li>Server queries the transaction receipt from Tempo</li>
          <li>Server verifies the receipt confirms successful execution</li>
          <li>Server verifies receipt matches the original request</li>
          <li>Server returns receipt (payment confirmed)</li>
        </ol>
        <t>This flow is useful when:</t>
        <ul>
          <li>The client wants to control transaction timing or gas settings</li>
          <li>The client is using a wallet that handles broadcasting internally</li>
        </ul>
        <t>Limitations:</t>
        <ul>
          <li>Cannot be used with <tt>feePayer: true</tt> (client must pay their own fees)</li>
          <li>Cannot be used for <tt>subscription</tt> intent (requires key authorization)</li>
          <li>Server cannot modify or enhance the transaction</li>
        </ul>
      </section>
      <section anchor="receipt-generation">
        <name>Receipt Generation</name>
        <t>Upon successful settlement, servers <bcp14>MUST</bcp14> return a <tt>Payment-Receipt</tt> header per Section 5.3 of [I-D.ietf-httpauth-payment].</t>
        <t>The receipt payload for Tempo:</t>
        <table>
          <thead>
            <tr>
              <th>Field</th>
              <th>Type</th>
              <th>Description</th>
            </tr>
          </thead>
          <tbody>
            <tr>
              <td><tt>method</tt></td>
              <td>string</td>
              <td><tt>&quot;tempo&quot;</tt></td>
            </tr>
            <tr>
              <td><tt>reference</tt></td>
              <td>string</td>
              <td>Transaction hash of the settlement transaction</td>
            </tr>
            <tr>
              <td><tt>status</tt></td>
              <td>string</td>
              <td><tt>&quot;success&quot;</tt> or <tt>&quot;failed&quot;</tt></td>
            </tr>
            <tr>
              <td><tt>timestamp</tt></td>
              <td>string</td>
              <td>ISO 8601 settlement time</td>
            </tr>
          </tbody>
        </table>
        <t>Example receipt payload:</t>
        <sourcecode type="json"><![CDATA[
{
  "method": "tempo",
  "reference": "0x1234567890abcdef...",
  "status": "success",
  "timestamp": "2026-01-06T12:00:00Z"
}
]]></sourcecode>
      </section>
    </section>
    <section anchor="internationalization-considerations">
      <name>Internationalization Considerations</name>

      <section anchor="address-encoding">
        <name>Address Encoding</name>
        <t>All Ethereum addresses in request and payload structures <bcp14>MUST</bcp14> be encoded as hexadecimal strings with <tt>0x</tt> prefix. Addresses are case-insensitive per [EIP-55], but implementations <bcp14>SHOULD</bcp14> use checksummed addresses for display purposes.</t>
      </section>
    </section>
    <section anchor="security-considerations">
      <name>Security Considerations</name>

      <section anchor="transaction-replay">
        <name>Transaction Replay</name>
        <t>Tempo Transactions include chain ID, nonce, and optional <tt>validBefore</tt>/ <tt>validAfter</tt> timestamps that prevent replay attacks:</t>
        <ul>
          <li>Chain ID binding prevents cross-chain replay</li>
          <li>Nonce consumption prevents same-chain replay</li>
          <li>Validity windows limit temporal replay windows</li>
        </ul>
      </section>
      <section anchor="access-key-security">
        <name>Access Key Security</name>
        <t>Access keys present additional security considerations:</t>
        <t><strong>Short Expiry for Charges</strong>: Access keys used for <tt>charge</tt> intent <bcp14>MUST</bcp14> have expiry within 5 minutes to minimize exposure window. Servers <bcp14>MUST</bcp14> reject access keys with longer expiry for charge intents.</t>
        <t><strong>Destination Scoping</strong>: For <tt>authorize</tt> and <tt>subscription</tt> intents, clients <bcp14>SHOULD</bcp14> include destination restrictions to limit the addresses the key can transfer to. This prevents key compromise from enabling transfers to attacker-controlled addresses.</t>
        <t><strong>Spending Limits</strong>: Access key spending limits are enforced by the AccountKeychain precompile onchain. Servers cannot exceed the authorized limits even if compromised.</t>
        <t><strong>Key Revocation</strong>: Users can revoke access keys at any time via the AccountKeychain precompile. Servers <bcp14>SHOULD</bcp14> handle revocation gracefully by requesting new authorization.</t>
      </section>
      <section anchor="amount-verification">
        <name>Amount Verification</name>
        <t>Clients <bcp14>MUST</bcp14> parse and verify the <tt>request</tt> payload before signing:</t>
        <ol type="1">
          <li>Verify <tt>amount</tt> is reasonable for the service</li>
          <li>Verify <tt>token</tt> is the expected token address</li>
          <li>Verify <tt>recipient</tt> is controlled by the expected party</li>
          <li>For <tt>authorize</tt>: verify <tt>expiry</tt> is not unreasonably far in the future</li>
          <li>For <tt>subscription</tt>: verify <tt>period</tt> and <tt>amount</tt> match expectations</li>
        </ol>
      </section>
      <section anchor="source-verification">
        <name>Source Verification</name>
        <t>If a credential includes the optional <tt>source</tt> field (a DID identifying the payer), servers <bcp14>MUST NOT</bcp14> trust this value without verification. The <tt>source</tt> field is client-provided metadata and could be forged.</t>
        <t>Servers <bcp14>MUST</bcp14> verify the payer identity by:</t>
        <ul>
          <li>For <tt>type=&quot;transaction&quot;</tt>: Recovering the signer address from the</li>
        </ul>
        <t>transaction signature using standard ECDSA recovery</t>
        <ul>
          <li>For <tt>type=&quot;keyAuthorization&quot;</tt>: Deriving the address from the <tt>publicKey</tt></li>
        </ul>
        <t>field in the key authorization</t>
        <ul>
          <li>For <tt>type=&quot;hash&quot;</tt>: Retrieving the <tt>from</tt> address from the transaction</li>
        </ul>
        <t>receipt onchain</t>
        <t>If <tt>source</tt> is present, servers <bcp14>SHOULD</bcp14> verify that the recovered/retrieved address matches the address in the DID (e.g., for <tt>did:pkh:eip155:42431:0xABC...</tt>, verify the address equals <tt>0xABC...</tt>). Servers <bcp14>MUST</bcp14> reject credentials where the <tt>source</tt> does not match the payer address.</t>
      </section>
      <section anchor="server-paid-fees-2">
        <name>Server-Paid Fees</name>
        <t>Servers acting as fee payers accept financial risk in exchange for providing a seamless payment experience.</t>
        <t><strong>Denial of Service</strong>: Malicious clients could submit valid-looking credentials that fail onchain, causing the server to pay fees without receiving payment. Servers <bcp14>SHOULD</bcp14> implement rate limiting and <bcp14>MAY</bcp14> require client authentication before accepting payment credentials.</t>
        <t><strong>Fee Token Exhaustion</strong>: Servers <bcp14>MUST</bcp14> monitor their fee token balance and reject new payment requests when balance is insufficient. Servers <bcp14>SHOULD</bcp14> alert operators when fee token balance falls below a threshold.</t>
      </section>
    </section>
    <section anchor="iana-considerations">
      <name>IANA Considerations</name>

      <section anchor="payment-method-registration">
        <name>Payment Method Registration</name>
        <t>This document registers the following payment method in the &quot;HTTP Payment Methods&quot; registry established by [I-D.ietf-httpauth-payment]:</t>
        <ul>
          <li><strong>Method Identifier</strong>: <tt>tempo</tt></li>
          <li><strong>Description</strong>: Tempo Network TIP-20 token payments</li>
          <li><strong>Reference</strong>: This document</li>
          <li><strong>Contact</strong>: jake@tempo.xyz</li>
        </ul>
      </section>
      <section anchor="payment-intent-registrations">
        <name>Payment Intent Registrations</name>
        <t>This document registers the following payment intents in the &quot;HTTP Payment Intents&quot; registry established by [I-D.ietf-httpauth-payment]:</t>
        <table>
          <thead>
            <tr>
              <th>Intent</th>
              <th>Applicable Methods</th>
              <th>Description</th>
              <th>Reference</th>
            </tr>
          </thead>
          <tbody>
            <tr>
              <td><tt>charge</tt></td>
              <td><tt>tempo</tt></td>
              <td>One-time TIP-20 transfer</td>
              <td>This document, Section 5.1</td>
            </tr>
            <tr>
              <td><tt>authorize</tt></td>
              <td><tt>tempo</tt></td>
              <td>Payment authorization</td>
              <td>This document, Section 5.2</td>
            </tr>
            <tr>
              <td><tt>subscription</tt></td>
              <td><tt>tempo</tt></td>
              <td>Recurring payment authorization</td>
              <td>This document, Section 5.3</td>
            </tr>
          </tbody>
        </table>
        <t>Note: <tt>charge</tt> is already registered as a base intent by [I-D.ietf-httpauth-payment]. This document extends its definition for the <tt>tempo</tt> method.</t>
      </section>
    </section>
  </middle>

  <back>
    <references>
      <name>References</name>
      <references>
        <name>Normative References</name>
        <xi:include href="https://bib.ietf.org/public/rfc/bibxml/reference.RFC.2119.xml"/>
        <xi:include href="https://bib.ietf.org/public/rfc/bibxml/reference.RFC.4648.xml"/>
        <xi:include href="https://bib.ietf.org/public/rfc/bibxml/reference.RFC.8174.xml"/>
        <xi:include href="https://bib.ietf.org/public/rfc/bibxml/reference.RFC.8259.xml"/>
      </references>
    </references>
    <section anchor="abnf-collected">
      <name>ABNF Collected</name>
      <t>This appendix collects the ABNF grammar for Tempo payment method structures.</t>
      <sourcecode type="abnf"><![CDATA[
; Request JSON structures (intent-specific)

; Charge request
charge-request = "{" charge-members "}"
charge-members = charge-amount "," charge-asset "," charge-destination ","
                 charge-expires
charge-amount = %s"amount" ":" number-string
charge-asset = %s"asset" ":" eth-address
charge-destination = %s"destination" ":" eth-address
charge-expires = %s"expires" ":" number-string

; Authorize request
authorize-request = "{" authorize-members "}"
authorize-members = authorize-asset "," [ authorize-destination "," ]
authorize-expires "," authorize-limit [ "," authorize-valid-from ]
authorize-asset = %s"asset" ":" eth-address
authorize-destination = %s"destination" ":" eth-address
authorize-expires = %s"expires" ":" number-string
authorize-limit = %s"limit" ":" number-string
authorize-valid-from = %s"validFrom" ":" number-string

; Subscription request
subscription-request = "{" subscription-members "}"
subscription-members = sub-amount "," sub-asset "," sub-expires ","
                       sub-period [ "," sub-valid-from ]
sub-amount = %s"amount" ":" number-string
sub-asset = %s"asset" ":" eth-address
sub-expires = %s"expires" ":" number-string
sub-period = %s"period" ":" number-string
sub-valid-from = %s"validFrom" ":" number-string

; Credential JSON structure
credential = "{" credential-members "}"
credential-members = credential-id "," credential-payload [ "," credential-source ]

credential-id = %s"id" ":" quoted-string
credential-payload = %s"payload" ":" payload-object
credential-source = %s"source" ":" quoted-string

; Payload object structure
payload-object = "{" payload-members "}"
payload-members = ( signature-payload / hash-payload )

; Signature-based payload (transaction or keyAuthorization)
signature-payload = payload-type-sig "," payload-signature
payload-type-sig = %s"type" ":" ( %s"\"transaction\"" / %s"\"keyAuthorization\"" )
payload-signature = %s"signature" ":" hex-string

; Hash-based payload (client-broadcast)
hash-payload = payload-type-hash "," payload-hash
payload-type-hash = %s"type" ":" %s"\"hash\""
payload-hash = %s"hash" ":" tx-hash

; Primitives
eth-address = DQUOTE "0x" 40HEXDIG DQUOTE
tx-hash = DQUOTE "0x" 64HEXDIG DQUOTE
hex-string = DQUOTE "0x" *HEXDIG DQUOTE
number-string = DQUOTE 1*DIGIT DQUOTE
quoted-string = DQUOTE *VCHAR DQUOTE
]]></sourcecode>
    </section>
    <section anchor="examples">
      <name>Examples</name>
      <t>The examples in this appendix use alphaUSD (<tt>0x20c0000000000000000000000000000000000001</tt>) as an illustrative TIP-20 asset. This is not a real token address.</t>
      <section anchor="charge">
        <name>Charge</name>
        <artwork type="ascii-art"><![CDATA[
   Client                        Server                     Tempo Network
      │                             │                             │
      │  (1) GET /api/resource      │                             │
      ├────────────────────────────>│                             │
      │                             │                             │
      │  (2) 402 Payment Required   │                             │
      │      intent="charge"        │                             │
      │<────────────────────────────┤                             │
      │                             │                             │
      │  (3) Sign transfer tx       │                             │
      │                             │                             │
      │  (4) Authorization: Payment │                             │
      ├────────────────────────────>│                             │
      │                             │  (5) Broadcast tx           │
      │                             ├────────────────────────────>│
      │                             │  (6) Transfer complete      │
      │                             │<────────────────────────────┤
      │  (7) 200 OK + Receipt       │                             │
      │<────────────────────────────┤                             │
      │                             │                             │
]]></artwork>
        <t>Challenge:</t>
        <sourcecode type="http"><![CDATA[
HTTP/1.1 402 Payment Required
WWW-Authenticate: Payment id="kM9xPqWvT2nJrHsY4aDfEb",
  realm="api.example.com",
  method="tempo",
  intent="charge",
  request="eyJhbW91bnQiOiIxMDAwMDAwIiwiYXNzZXQiOiIweDIwYzAwMDAwMDAwMDAwMDAwMDAwMDAwMDAwMDAwMDAwMDAwMDAwMDAiLCJkZXN0aW5hdGlvbiI6IjB4NzQyZDM1Q2M2NjM0QzA1MzI5MjVhM2I4NDRCYzllNzU5NWY4ZkUwMCIsImV4cGlyZXMiOiIyMDI1LTAxLTA2VDEyOjAwOjAwWiJ9"
]]></sourcecode>
        <t>The <tt>request</tt> decodes to:</t>
        <sourcecode type="json"><![CDATA[
{
  "amount": "1000000",
  "asset": "0x20c0000000000000000000000000000000000001",
  "destination": "0x742d35Cc6634C0532925a3b844Bc9e7595f8fE00",
  "expires": "2025-01-06T12:00:00Z"
}
]]></sourcecode>
        <t>This requests a transfer of 1.00 alphaUSD (1000000 base units).</t>
        <t>Credential (Signed Transaction):</t>
        <sourcecode type="http"><![CDATA[
GET /api/resource HTTP/1.1
Host: api.example.com
Authorization: Payment eyJpZCI6ImtNOXhQcVd2VDJuSnJIc1k0YURmRWIiLCJ0eXBlIjoidHJhbnNhY3Rpb24iLCJwYXlsb2FkIjoiMHg3NmY5MDEuLi4ifQ
]]></sourcecode>
        <t>The credential decodes to:</t>
        <sourcecode type="json"><![CDATA[
{
  "id": "kM9xPqWvT2nJrHsY4aDfEb",
  "payload": {
    "signature": "0x76f901...signed transaction bytes...",
    "type": "transaction"
  },
  "source": "did:pkh:eip155:42431:0x1234567890abcdef1234567890abcdef12345678"
}
]]></sourcecode>
        <t>The client constructs a Tempo Transaction calling <tt>transfer(destination, amount)</tt> on the asset, fills in <tt>nonce</tt>, <tt>nonceKey</tt>, and <tt>signature</tt>, then RLP-serializes.</t>
        <t>Credential (Transaction Hash):</t>
        <t>Alternatively, the client can broadcast the transaction themselves and submit the hash:</t>
        <sourcecode type="json"><![CDATA[
{
  "id": "kM9xPqWvT2nJrHsY4aDfEb",
  "payload": {
    "hash": "0x1a2b3c4d5e6f7890abcdef1234567890abcdef1234567890abcdef1234567890",
    "type": "hash"
  },
  "source": "did:pkh:eip155:42431:0x1234567890abcdef1234567890abcdef12345678"
}
]]></sourcecode>
        <t>The client constructs and broadcasts the <tt>transfer</tt> transaction to Tempo, waits for confirmation, then submits the transaction hash. The server verifies the transaction onchain matches the request parameters.</t>
        <t>Response with receipt:</t>
        <sourcecode type="http"><![CDATA[
HTTP/1.1 200 OK
Payment-Receipt: eyJtZXRob2QiOiJ0ZW1wbyIsInR4SGFzaCI6IjB4MTIzNDU2Nzg5MGFiY2RlZi4uLiIsImJsb2NrTnVtYmVyIjoxMjM0NTY3OCwiYW1vdW50IjoiMTAwMDAwMCJ9
Content-Type: application/json

{ "data": "..." }
]]></sourcecode>
      </section>
      <section anchor="authorize">
        <name>Authorize</name>
        <artwork type="ascii-art"><![CDATA[
   Client                        Server                     Tempo Network
      │                             │                             │
      │  (1) GET /api/resource      │                             │
      ├────────────────────────────>│                             │
      │                             │                             │
      │  (2) 402 Payment Required   │                             │
      │      intent="authorize"       │                             │
      │<────────────────────────────┤                             │
      │                             │                             │
      │  (3) Sign keyAuthorization  │                             │
      │                             │                             │
      │  (4) Authorization: Payment │                             │
      ├────────────────────────────>│                             │
      │                             │  (5) Store keyAuth          │
      │  (6) 200 OK (approved)      │                             │
      │<────────────────────────────┤                             │
      │                             │                             │
      │         ... later ...       │                             │
      │                             │  (7) Charge with keyAuth    │
      │                             ├────────────────────────────>│
      │                             │  (8) Transfer complete      │
      │                             │<────────────────────────────┤
      │                             │                             │
]]></artwork>
        <t>Challenge:</t>
        <sourcecode type="http"><![CDATA[
HTTP/1.1 402 Payment Required
WWW-Authenticate: Payment id="nR5tYuLpS8mWvXzQ1eCgHj",
  realm="api.example.com",
  method="tempo",
  intent="authorize",
  request="eyJhc3NldCI6IjB4MjBjMDAwMDAwMDAwMDAwMDAwMDAwMDAwMDAwMDAwMDAwMDAwMDAwMCIsImV4cGlyZXMiOiIyMDI1LTAyLTA1VDEyOjAwOjAwWiIsImxpbWl0IjoiNTAwMDAwMDAifQ"
]]></sourcecode>
        <t>The <tt>request</tt> decodes to:</t>
        <sourcecode type="json"><![CDATA[
{
  "asset": "0x20c0000000000000000000000000000000000001",
  "expires": "2025-02-05T12:00:00Z",
  "limit": "50000000"
}
]]></sourcecode>
        <t>This requests approval for up to 50.00 alphaUSD (50000000 base units).</t>
        <t>Credential (via Key Authorization):</t>
        <sourcecode type="json"><![CDATA[
{
  "id": "nR5tYuLpS8mWvXzQ1eCgHj",
  "payload": {
    "signature": "0xf8b2...signed authorization bytes...",
    "type": "keyAuthorization"
  },
  "source": "did:pkh:eip155:42431:0x1234567890abcdef1234567890abcdef12345678"
}
]]></sourcecode>
        <t>Credential (via Transaction):</t>
        <t>Alternatively, when the request includes a <tt>destination</tt> field, the client may fulfill the approval using a Tempo Transaction with an <tt>approve</tt> call:</t>
        <sourcecode type="json"><![CDATA[
{
  "id": "nR5tYuLpS8mWvXzQ1eCgHj",
  "payload": {
    "signature": "0x76f901...signed transaction bytes...",
    "type": "transaction"
  },
  "source": "did:pkh:eip155:42431:0x1234567890abcdef1234567890abcdef12345678"
}
]]></sourcecode>
        <t>The transaction contains <tt>approve(destination, limit)</tt> on the TIP-20 asset. The server broadcasts this transaction to register the allowance onchain, then later calls <tt>transferFrom</tt> to collect payment.</t>
        <t>Credential (via Transaction Hash):</t>
        <t>The client can also broadcast an <tt>approve</tt> transaction themselves:</t>
        <sourcecode type="json"><![CDATA[
{
  "id": "nR5tYuLpS8mWvXzQ1eCgHj",
  "payload": {
    "hash": "0x9f8e7d6c5b4a3210fedcba0987654321fedcba0987654321fedcba0987654321",
    "type": "hash"
  },
  "source": "did:pkh:eip155:42431:0x1234567890abcdef1234567890abcdef12345678"
}
]]></sourcecode>
        <t>The client constructs and broadcasts an <tt>approve(destination, limit)</tt> transaction to Tempo, then submits the hash. The server verifies the approval was registered onchain before granting access.</t>
      </section>
      <section anchor="subscription">
        <name>Subscription</name>
        <artwork type="ascii-art"><![CDATA[
   Client                        Server                     Tempo Network
      │                             │                             │
      │  (1) GET /api/resource      │                             │
      ├────────────────────────────>│                             │
      │                             │                             │
      │  (2) 402 Payment Required   │                             │
      │      intent="subscription"  │                             │
      │<────────────────────────────┤                             │
      │                             │                             │
      │  (3) Sign keyAuthorization  │                             │
      │      (periodic limits)      │                             │
      │                             │                             │
      │  (4) Authorization: Payment │                             │
      ├────────────────────────────>│                             │
      │                             │  (5) Register + charge      │
      │                             ├────────────────────────────>│
      │                             │  (6) First period paid      │
      │                             │<────────────────────────────┤
      │  (7) 200 OK + Receipt       │                             │
      │<────────────────────────────┤                             │
      │                             │                             │
      │         ... 30 days ...     │                             │
      │                             │  (8) Charge next period     │
      │                             ├────────────────────────────>│
      │                             │  (9) Transfer complete      │
      │                             │<────────────────────────────┤
      │                             │                             │
]]></artwork>
        <t>Challenge:</t>
        <sourcecode type="http"><![CDATA[
HTTP/1.1 402 Payment Required
WWW-Authenticate: Payment id="qT8wErYuI3oPlKjH6gFdSa",
  realm="api.example.com",
  method="tempo",
  intent="subscription",
  request="eyJhbW91bnQiOiIxMDAwMDAwMCIsImFzc2V0IjoiMHgyMGMwMDAwMDAwMDAwMDAwMDAwMDAwMDAwMDAwMDAwMDAwMDAwMDAwIiwiZXhwaXJlcyI6IjIwMjYtMDEtMDZUMDA6MDA6MDBaIiwicGVyaW9kIjoiMjU5MjAwMCJ9"
]]></sourcecode>
        <t>The <tt>request</tt> decodes to:</t>
        <sourcecode type="json"><![CDATA[
{
  "amount": "10000000",
  "asset": "0x20c0000000000000000000000000000000000001",
  "expires": "2026-01-06T00:00:00Z",
  "period": "2592000"
}
]]></sourcecode>
        <t>This requests a subscription for 10.00 alphaUSD (10000000 base units) per 30 days (2592000 seconds).</t>
        <t>Credential:</t>
        <sourcecode type="json"><![CDATA[
{
  "id": "qT8wErYuI3oPlKjH6gFdSa",
  "payload": {
    "signature": "0xf8c1...signed authorization bytes...",
    "type": "keyAuthorization"
  }
}
]]></sourcecode>
        <t>Note: Periodic limit enforcement for subscriptions is configured when registering the key with the AccountKeychain precompile.</t>
      </section>
    </section>
    <section anchor="acknowledgements" numbered="false">
      <name>Acknowledgements</name>
      <t>The authors thank the Tempo community for their feedback on this specification.</t>
    </section>
  </back>

</rfc>