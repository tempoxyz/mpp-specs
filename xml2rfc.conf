# xml2rfc configuration file
# See: https://github.com/ietf-tools/xml2rfc

# Silence common warnings
# Note: ConfigArgParse requires JSON array format for multiple values
silence = ["Unused reference", "Artwork too wide", "Section too wide", "Too long line", "Middle too wide", "Back too wide"]
