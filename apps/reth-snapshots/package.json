{
	"name": "@ai-payments/reth-snapshots",
	"version": "0.0.1",
	"private": true,
	"type": "module",
	"scripts": {
		"dev": "wrangler dev",
		"deploy": "wrangler deploy",
		"check:types": "tsc --noEmit",
		"deploy:preview": "wrangler deploy --env preview",
		"deploy:moderato": "wrangler deploy --env moderato",
		"deploy:presto": "wrangler deploy --env presto"
	},
	"dependencies": {
		"@ai-payments/protocol": "workspace:*",
		"hono": "catalog:",
		"viem": "catalog:"
	},
	"devDependencies": {
		"@cloudflare/workers-types": "catalog:",
		"typescript": "catalog:",
		"wrangler": "catalog:"
	}
}
