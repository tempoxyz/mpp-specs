# MPP Test Vectors

Machine-readable conformance vectors shared by MPP implementations.

`core-hmac-sha256.json` covers the recommended challenge ID binding in the
core specification. Implementations should consume this file directly in
their test suites. The specification is authoritative if the two disagree.
